<%= turbo_frame_tag :list do %>  
  <div id="list_items" class="h-screen overflow-y-auto" data-controller="list" data-list-active-class="bg-blue-100" data-preserve-scroll>
    <div class="sticky top-0 bg-white p-4 flex items-center justify-center border-b text-2xl font-bold z-10">
      <h1>Inbox</h1>
    </div>
    <div class="flex flex-col divide-y divide-gray-200">
      <%= render @emails %>
    </div>
  </div>
<% end %>